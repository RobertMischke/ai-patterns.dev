{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ai-patterns.dev/schemas/concept-article.schema.json",
  "title": "ConceptArticle",
  "description": "Long-form article for a concept. Optional sibling to concept.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["lede", "body"],
  "properties": {
    "lede": { "type": "string", "minLength": 1 },
    "body": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["h", "p"],
        "properties": {
          "h": { "type": "string", "minLength": 1, "description": "Section heading." },
          "p": { "type": "string", "minLength": 1, "description": "Section paragraph." }
        }
      }
    }
  }
}
